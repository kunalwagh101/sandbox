name: Verify delivery evidence

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Verify board and evidence
        run: python scripts/verify_board.py
      - name: Run verifier tests
        run: python -m unittest discover -s tests -v
      - name: Execute portable PowerShell security behaviour
        shell: pwsh
        run: |
          ./tests/Invoke-SourceAcceptance.ps1
          ./tests/Invoke-CompatibilityAcceptance.ps1
          ./tests/Invoke-LifecycleAcceptance.ps1

  powershell-source:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Parse every PowerShell file with Windows PowerShell
        shell: powershell
        run: |
          $allErrors = @()
          Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
            $tokens = $null
            $parseErrors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $_.FullName,
              [ref]$tokens,
              [ref]$parseErrors
            ) | Out-Null
            $allErrors += $parseErrors
          }
          if ($allErrors.Count -gt 0) {
            $allErrors | Format-List -Force
            throw "$($allErrors.Count) PowerShell parse error(s)"
          }
      - name: Execute PowerShell security behaviour with Windows PowerShell 5.1
        shell: powershell
        run: |
          .\tests\Invoke-SourceAcceptance.ps1
          .\tests\Invoke-CompatibilityAcceptance.ps1
          .\tests\Invoke-LifecycleAcceptance.ps1
      - name: Run repository tests
        run: python -m unittest discover -s tests -v
